model Product {
  id          String      @id @default(uuid())
  name        String      @unique()
  description String?
  image       String?
  // in cents
  price       Int
  items       OrderItem[]

  @@map("products")
}
